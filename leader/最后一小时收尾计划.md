# 最后一小时收尾计划

更新时间：2026-05-10 14:20 CST

## 当前结论

Agent D 在 14:09-14:14 的测试表明：项目后端、前端、KG、Merge、RAG、报告生成主链路大体可跑，但现在仍不可演示、不可部署。阻断项只剩三个必须处理：

1. `/api/feedback` 对 `{ "instruction": "保留 函数" }` 返回 400，教师反馈链路失败。
2. `textbooks/03_生理学.pdf` 原始中文文件名上传返回 400 `Unsupported file type`，中文教材原文件无法导入。
3. 教材列表仍显示 `textbook_math_001`、`textbook_physics_001` 这类演示/假教材，不符合展示要求。

次级问题：

1. RAG 后端日志有 `kg_edges.json/kg_nodes.json: 'list' object has no attribute 'get'`，接口 200，但应尽量静默兼容。
2. 前端实际端口是 `http://localhost:3000/`，不是 5173，需要统一文档和演示口径。
3. 测试窗口期间出现大量 Git 变更，最终必须做一次冻结窗口复测。

## 时间划分

### 14:20-14:35：只修阻断项

Agent A、B、C 同时开工，但写入范围必须严格隔离。不要再做大重构、不要改 UI 风格、不要改 parser/storage 核心逻辑，除非是中文文件名上传的最小修复。

### 14:35-14:45：代码冻结和快速自检

各 agent 提交自己的变更摘要，确认没有互相覆盖。Leader 汇总变更范围。此阶段之后除非阻断项未修，不再接受新增功能改动。

### 14:45-14:58：Agent D 做最终复测

Agent D 只测不改，使用 `edu` conda 环境和 `npm.cmd`。重点测 3 个阻断项和演示主链路。

### 14:58-15:05：演示准备

只做演示口径、README/leader 文档更新、截图或录屏补充。不要再改功能代码。

## Agent A 提示词：反馈链路和 RAG 数据兼容

你是 Agent A，负责最后一小时后端阻断修复。当前比赛还剩约 1 小时，只允许做最小修复，不要重构。

背景：
- Agent D 测试发现 `POST /api/feedback` 使用请求体 `{ "instruction": "保留 函数" }` 返回 400。
- 之前日志出现过 422，但现在是 400，说明请求体字段已基本对齐，问题更可能在中文反馈指令解析逻辑。
- 代码中反馈指令正则疑似 mojibake，不能匹配真实中文“保留 函数”。
- RAG 查询接口 200，但后端日志有 `kg_edges.json/kg_nodes.json: 'list' object has no attribute 'get'`，说明读取 KG JSON 时没有兼容 list/dict 两种结构。

负责文件：
- `backend/services/feedback.py`
- `backend/routers/feedback.py`
- `backend/services/rag.py`
- 必要时只读 `backend/routers/rag.py`

不要修改：
- 前端文件
- `backend/services/parser.py`
- `backend/services/storage.py`
- `textbooks/`
- `problem/`

必须完成：
1. 修复教师反馈中文指令解析，让以下请求返回 200：
   ```json
   { "instruction": "保留 函数" }
   ```
2. 至少兼容这些中文动作：`保留`、`删除`、`修改`、`合并`、`拆分`。目标词可以是中文、英文或混合词。
3. 如果指令无法解析，返回清晰的 400 message，不要抛 500。
4. 修复 RAG 读取 `kg_nodes.json`、`kg_edges.json` 时 list/dict 不兼容导致的日志报错。无论文件是 `[...]` 还是 `{ "nodes": [...] }` / `{ "edges": [...] }` 都应兼容。
5. 不改变接口字段名，`/api/feedback` 仍优先接受 `instruction`。

验收命令由 Agent D 执行，你只需要给出变更摘要和建议测试点。

验收标准：
- `POST /api/feedback` with `{ "instruction": "保留 函数" }` 返回 200。
- `/api/rag/query` 返回 200，后端日志不再出现 `list object has no attribute get`。
- 不引入新的 import error。

## Agent B 提示词：中文文件名 PDF 上传

你是 Agent B，负责最后一小时后端上传阻断修复。当前比赛还剩约 1 小时，只允许做最小修复，不要重构解析器。

背景：
- Agent D 测试发现 `textbooks/03_生理学.pdf` 原始中文文件名上传返回 400：`Unsupported file type`。
- 同一个 PDF 复制为 ASCII 文件名 `reference_03_physiology.pdf` 后上传 200，解析 200。
- 因此问题不是 PDF 内容，而是上传文件名/扩展名判断逻辑对中文路径或编码处理不兼容。

负责文件：
- `backend/routers/upload.py`
- 必要时只读 `backend/services/parser.py`
- 必要时只读 `backend/services/storage.py`

不要修改：
- 前端文件
- KG/Merge/RAG/Feedback/Report 功能
- `textbooks/` 原始文件

必须完成：
1. 修复上传文件类型判断，中文文件名如 `03_生理学.pdf` 必须识别为 PDF。
2. 文件类型判断应基于安全的扩展名解析，例如 `Path(filename).suffix.lower()` 或等价方式，不要用会被中文编码影响的字符串切分。
3. 保存文件时保留或安全处理原始文件名，但不能因为中文名导致 400。
4. 只做 PDF/TXT/DOCX 等现有支持类型的兼容，不新增复杂格式。
5. 保持现有上传接口返回结构不变。

验收标准：
- 上传 `textbooks/03_生理学.pdf` 返回 200。
- 随后解析该教材返回 200。
- ASCII 文件名上传仍然返回 200。

## Agent C 提示词：假教材清理、前端端口口径和演示稳定性

你是 Agent C，负责最后一小时展示层和数据展示修复。当前比赛还剩约 1 小时，只做最小改动，不要重做 UI。

背景：
- Agent D 测试发现 `/api/textbooks` 返回 `textbook_math_001`、`textbook_physics_001` 这类疑似演示/假教材。
- 用户明确要求默认教材不要奇怪，应该使用 `textbooks/` 目录中的真实参考教材，其他教材可手动导入。
- 前端启动实际端口是 `http://localhost:3000/`，不是 5173。需要统一说明，避免演示时找错地址。
- Build 已通过，仅有 chunk size warning。

负责文件：
- `frontend/src/api.js`
- `frontend/src/App.jsx`
- `frontend/src/components/UploadPanel.jsx`
- 必要时 `frontend/src/components/*.jsx` 中与教材列表显示相关的最小改动
- `frontend/package.json` 或前端说明文档中端口相关内容
- 必要时只读 `backend/routers/textbooks.py`

不要修改：
- 后端上传、反馈、RAG 核心逻辑
- KG/Merge 算法
- `textbooks/` 原始文件

必须完成：
1. 前端不要把 `textbook_math_001`、`textbook_physics_001` 这类 mock/demo 教材作为默认展示重点。
2. 如果后端已经返回这些 demo 项，前端可以在展示层过滤或弱化它们，但不要破坏真实上传教材显示。
3. 手动上传/解析后，左侧教材列表必须刷新，能显示新教材、解析状态、章节/字数等关键结果。
4. 更新启动口径：本项目当前前端演示地址是 `http://localhost:3000/`。如果配置里确实固定 3000，就文档写 3000；如果要改到 5173，必须确保 `npm.cmd run dev` 实际启动也是 5173。当前建议不要改端口，只统一文档。
5. 确保报告详情、RAG 面板、图谱面板不要因为空字段白屏。不要做大规模视觉调整，除非是明显异常。

验收标准：
- 首屏教材列表不再把假教材作为默认可信内容展示。
- 上传/解析新教材后左侧列表立刻刷新。
- 前端说明与实际端口一致：`http://localhost:3000/`。
- `npm.cmd run build` 通过。

## Agent D 提示词：最终冻结复测，只测不改

你是 Agent D，负责最后 15 分钟最终验收。你只测试，不修改任何代码或数据源配置。测试前请确认 A/B/C 都已经说“完成”，然后进入冻结窗口。

环境：
- 工作目录：`D:\我的大学\博一下\黑客松2`
- 后端 Python：`D:\software\anaconda\envs\edu\python.exe`
- 前端 npm：`C:\Program Files\nodejs\npm.cmd`
- 后端启动：
  ```powershell
  cd "D:\我的大学\博一下\黑客松2"
  & "D:\software\anaconda\envs\edu\python.exe" run.py
  ```
- 前端启动：
  ```powershell
  cd "D:\我的大学\博一下\黑客松2\frontend"
  & "C:\Program Files\nodejs\npm.cmd" run dev
  ```
- 当前前端地址按实际输出记录，预期是 `http://localhost:3000/`。

冻结要求：
1. 测试期间任何 agent 不得再改代码。
2. 测试前记录 `git status --short`。
3. 测试后再次记录 `git status --short`。
4. 如果只有 `data/processed/*`、`report/整合报告.md`、`test_artifacts/*` 因测试链路变化，标记为“测试产物变化”；如果功能代码变化，标记冻结失败。

必测项：
1. `/health` 返回 200。
2. 前端 dev server 能打开，记录实际端口。
3. `/api/textbooks` 返回 200，首屏不再突出展示假教材。
4. 上传 `textbooks/03_生理学.pdf` 原始中文文件名，必须返回 200。
5. 解析该中文文件名教材，必须返回 200，并记录章节数/字数。
6. `/api/kg/build`、`/api/kg` 返回 200，记录 nodes/edges。
7. `/api/merge`、`/api/merge/decisions` 返回 200，记录 decisions 数量和 compression_ratio。
8. `/api/rag/index`、`/api/rag/query` 返回 200，记录 answer 长度和 citations 数量；检查后端日志不再有 `list object has no attribute get`。
9. `/api/feedback` 使用 `{ "instruction": "保留 函数" }` 返回 200。
10. `/api/report/generate`、`/api/report/latest` 返回 200，latest 必须包含 Markdown 正文。
11. `npm.cmd run build` 退出码 0，chunk size warning 可接受。

输出格式：
- 测试时间。
- Git 冻结是否满足。
- 每项接口状态码和简短结果。
- 前端端口。
- 是否可演示。
- 是否可部署到公网。
- 剩余风险，不超过 5 条。

最终判定标准：
- 反馈 200、中文文件名上传 200、假教材不再作为默认展示重点，则可演示。
- build 通过、后端主链路通过，则可准备公网部署。

